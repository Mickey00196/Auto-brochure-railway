{
  "manifest_version": 3,
  "name": "Proposal Engine – Listing Capture",
  "version": "1.1.0",
  "description": "Capture the listing page you are already viewing into the Proposal Engine — one click, no retyping. Reads the open page only; it does not fetch anything itself.",
  "action": {
    "default_popup": "popup.html",
    "default_title": "Capture this listing"
  },
  "permissions": ["activeTab", "scripting"]
}
